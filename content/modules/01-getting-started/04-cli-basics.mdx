---
title: "CLI Navigation & Basics"
description: "The shell commands, session commands, and shortcuts worth committing to memory — explained for a first-time terminal user."
order: 4
duration: "10 min"
coverImage: "https://images.unsplash.com/photo-1601882589907-ce1b7efe2a40?w=1200&auto=format&fit=crop&q=80"
quiz:
  - question: "You finished a session yesterday and want to pick the same conversation back up in the same directory. What's the fastest route?"
    options:
      - "You can't — sessions don't persist, so summarize what you were doing in a fresh session"
      - "`claude -c` continues the most recent conversation in the current directory, or `claude -r` / `/resume` picks a specific one"
      - "Re-run `claude` with the same prompt to rebuild the conversation"
      - "Copy the transcript out of ~/.claude and paste it back in"
    answer: 1
  - question: "You've been on the same debugging task for 40 minutes and context is filling up. Which command fits, and why?"
    options:
      - "`/clear`, because it frees the most space"
      - "`/compact`, because it summarizes the conversation and keeps what matters — you still need the earlier findings"
      - "`/exit` and restart, because a fresh process is always cleaner"
      - "`/config`, to raise the context limit"
    answer: 1
  - question: "What does `Shift+Tab` do in a session?"
    options:
      - "Autocompletes the current command"
      - "Cycles permission modes"
      - "Switches between open files"
      - "Toggles the transcript viewer"
    answer: 1
---

## You Do Not Need The Whole Manual

A **flag** is an extra word after a command that changes how it runs. In `claude -c`, the `-c` is a flag. You do not need to memorize every flag. You need four moves: start a session, ask one question and quit, pick up yesterday's session, and free up a long conversation.

Claude Code runs from your terminal as either a conversation or a single shot. Knowing which you want before you type saves a surprising amount of time.

**Shell commands** — these start or resume Claude Code from your terminal:

| Command | What it does | Example |
|---|---|---|
| `claude` | Start interactive mode | `claude` |
| `claude "task"` | Run a one-time task | `claude "fix the typo in hello.md"` |
| `claude -p "query"` | Run one-off query, then exit | `claude -p "explain this file"` |
| `claude -c` | Continue most recent conversation in current directory | `claude -c` |
| `claude -r` | Resume a previous conversation | `claude -r` |

`claude "task"` and `claude -p "query"` look similar and aren't. The first opens an interactive session with that as the opening prompt. The second answers and exits — no session, no back-and-forth.

## Sessions Persist

This trips up people coming from older tutorials, so let's be direct: **your conversations don't evaporate when you exit.**

```bash
claude -c          # continue the most recent conversation in this directory
claude -r          # resume a previous conversation
```

`-c` is the one you'll use daily. You close your laptop, come back after lunch, `claude -c`, and you're back in the same conversation. Inside a session, `/resume` does the same job.

<Callout type="tip">`claude -c` is directory-scoped — it continues the most recent conversation *in the current folder*. If you jump between folders, `-c` gives you the right thread per folder automatically.</Callout>

## Session Commands

Once you're inside a session, lines that start with `/` talk to the tool:

| Command | What it does |
|---|---|
| `/help` | Show available commands |
| `/clear` | Clear conversation history |
| `/exit` (or `Ctrl+D`) | Exit Claude Code |
| `/status` | Show the active account and auth method |
| `/config` | Open configuration |
| `/model` | See and change the current model |
| `/resume` | Continue a previous conversation |
| `/compact` | Summarize the conversation to free up context |
| `/permissions` | Manage permission rules |

Type `/` on its own to see everything you can invoke.

## /compact vs /clear

These are not interchangeable, and picking wrong costs you real work.

- **`/compact`** summarizes the conversation and keeps going. Use it when you're deep in one task and running long — you need the earlier findings, just not every word of them.
- **`/clear`** wipes conversation history and starts fresh. Use it when you're *switching tasks*. Stale context from an unrelated problem doesn't just waste space; it actively pulls Claude toward irrelevant patterns.

The rule of thumb: **same task, `/compact`. Different task, `/clear`.**

<Callout type="info">Running `/clear` starts a new session, but the previous conversation is preserved and can be resumed with `/resume`. `/clear` isn't destructive to your history — it's destructive to your *current* context, which is the point.</Callout>

## Shortcuts Worth Memorizing

| Shortcut | What it does |
|---|---|
| `/` | List commands and skills |
| `@` | File path autocomplete |
| `Tab` | Complete |
| `↑` | Command history |
| `Shift+Tab` | Cycle permission modes |
| `Esc` | Interrupt Claude mid-turn |
| `Ctrl+D` | Exit |

`Shift+Tab` is the one that changes how you work rather than how fast you type — it's the whole subject of the next lesson.

`Esc` is the underrated one. When Claude is heading somewhere wrong, interrupt it. The work done so far is kept, and you redirect from there.

## Piping and Scripting

Skip this section until you are comfortable. `-p` is where Claude Code can read the output of another command and then exit:

```bash
git diff | claude -p "explain this change in plain language"
```

It reads stdin and writes stdout. This is the door to later automation. You do not need it for the capstone.

<Callout type="warning">Think carefully before wiring `-p` into anything automated. A non-interactive run has no one to answer permission prompts — which means you need to have made deliberate decisions about what it's allowed to do, in advance. That's the next lesson, and it's not optional reading before you put this in a pipeline.</Callout>

## Models

`/model` shows and changes the model for the current session. That's the mechanism you want — not a model name memorized from a course.

We're not naming a default model anywhere in this material. Defaults change without notice, and a lesson that confidently names one is wrong within weeks. `/model` is always right; a name in a tutorial has a shelf life.

<KeyPoint>Learn four things and you have the CLI: `claude` for a session, `-p` for one-shot and scripting, `-c` to pick up where you left off, and `/compact` vs `/clear` to manage context deliberately. Everything else you can find with `/help`.</KeyPoint>

Commands and shortcuts here come from the official [CLI reference](https://code.claude.com/docs/en/cli-reference), [commands reference](https://code.claude.com/docs/en/commands), and [interactive mode](https://code.claude.com/docs/en/interactive-mode) docs as verified for this course. Claude Code ships weekly and the command set grows — `/help` and the docs are the current truth.

Next, the lesson that keeps all of this from hurting you: permission modes.
