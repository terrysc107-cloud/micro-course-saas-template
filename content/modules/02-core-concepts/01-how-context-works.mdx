---
title: "How Context Works"
description: "Understand what Claude Code can see, why context matters, and how to manage it."
order: 1
duration: "8 min"
coverImage: "https://images.unsplash.com/photo-1558494949-ef010cbdcc31?w=1200&auto=format&fit=crop&q=80"
quiz:
  - question: "What is the context window in Claude Code?"
    options:
      - "A graphical window that shows your code"
      - "The total amount of text Claude can process in one session"
      - "The list of recently opened files"
      - "A browser window for viewing documentation"
    answer: 1
  - question: "When you start a Claude Code session, what does it automatically read?"
    options:
      - "Your entire hard drive"
      - "Only the file you have open in your editor"
      - "Your project directory structure and key files"
      - "Only files you explicitly name"
    answer: 2
  - question: "What happens when the context window fills up?"
    options:
      - "Claude Code crashes"
      - "Older parts of the conversation are forgotten or quality degrades"
      - "It automatically upgrades to a bigger model"
      - "It saves everything to disk and continues"
    answer: 1
  - question: "Which slash command helps free up context during a long session?"
    options:
      - "/save"
      - "/forget"
      - "/compact"
      - "/shrink"
    answer: 2
---

## What Is Context?

You do not need to already write code for this lesson. Context is just "what Claude can see right now." The same idea applies to a tiny practice folder and to a large app.

Context is everything Claude Code knows right now. It includes:

- Your conversation history (every message back and forth)
- File contents you've referenced or that were auto-loaded
- Any text Claude has written or modified in this session

Think of it like working memory. The more you cram in, the harder it is to think clearly about any one thing.

<Callout type="info">Context is not just conversation text — every file Claude reads gets added to the token count. A single 500-line file can consume more context than 20 back-and-forth messages, so loading files strategically is just as important as keeping conversations concise.</Callout>

## The Context Window

Claude Code runs on large language models with a finite context window — measured in tokens (roughly 0.75 words per token). When that window fills up:

- Older parts of the conversation may be dropped
- Claude may lose track of earlier decisions
- Responses can become less coherent or start making mistakes

Modern Claude models have very large context windows (200,000+ tokens), but long sessions with many file edits can still fill them.

## What Gets Loaded Automatically

When you start `claude` in a folder, it automatically loads:

1. **Project structure** — the file/folder tree
2. **`CLAUDE.md`** — if it exists (more on this in Module 5)
3. **`package.json`, `tsconfig.json`** — config files that reveal the stack, when they exist
4. **Git status** — what files are modified

It does NOT load every source file automatically — that would be too expensive. You load specific files by referencing them.

## Loading Files Explicitly

Use `@` syntax to tell Claude exactly what to look at:

```
> Look at @hello.md and explain what it says
> Compare @index.html and @app.js
> I am editing @app.js — here is the bug I am seeing
```

The same `@` move works on any stack. The checkout-flow example below is an illustration, not a requirement.

## Context Strategy

**Do:**
- Load only files relevant to the current task
- Use `/compact` when the conversation is getting long
- Use `/clear` between unrelated tasks
- Start new sessions for new pieces of work

<Callout type="tip">Do not wait for Claude to start making mistakes before running `/compact` — use it proactively after 20-30 minutes on a complex task. Running it while the conversation is still clean produces a much better summary than running it when Claude is already starting to drift.</Callout>

**Don't:**
- Load your entire folder at once
- Let sessions run for hours across many different tasks
- Ask Claude to "look at everything" in a large repo

## Practical Example

If you later work on a checkout flow, instead of:

```
> Read all the files in my project and find the bug
```

Do this:

```
> I have a bug where checkout fails with a 500 error.
  The relevant files are @src/api/checkout.ts and @src/lib/stripe.ts
  Here is the error from the server logs: [paste error]
```

This loads exactly the right context, nothing more. Today, the same pattern is: name `@hello.md` or `@app.js`, not "read everything."

<Callout type="warning">Watch out for large auto-generated files like `package-lock.json`, compiled output, or massive schema files — if Claude loads one of these, it can eat a huge chunk of your context window in a single step. Mention explicitly that Claude should ignore these files when relevant.</Callout>

## Summary

Context is your most important resource in Claude Code. Treat it like working memory — use it deliberately, free it up often, and your sessions will stay clearer.

<KeyPoint>Context is a finite resource — load only the files relevant to your current task, use `/compact` proactively in long sessions, and start a fresh session for each new task to keep Claude focused and your costs down.</KeyPoint>
